import assert from 'power-assert';
import { lengthOfLastWord } from '../lengthOfLastWord';

describe('src/lengthOfLastWord', () => {
  describe('lengthOfLastWord', () => {
    it('Hello World', () => {
      assert.strictEqual(lengthOfLastWord('Hello World'), 5);
    });
  });
});
