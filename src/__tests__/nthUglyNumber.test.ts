import assert from 'power-assert';
import { nthUglyNumber } from '../nthUglyNumber';

describe('src/nthUglyNumber', () => {
  describe('nthUglyNumber', () => {
    it('10', () => assert.strictEqual(nthUglyNumber(10), 12));
  });
});
